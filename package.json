{
  "name": "codex-remote",
  "version": "0.1.0",
  "private": true,
  "description": "A self-hosted browser gateway for local and SSH-connected Codex conversations.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/config.test.js"
  }
}
